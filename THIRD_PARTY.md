# 第三方素材与数据说明

本仓库**只包含原创代码与文档**。下列内容**有意未包含**，使用者需自行获取，
并自行承担相应的许可与合规责任。

---

## 一、CS2 饰品图片（未包含，且不可再分发）

| 项目 | 说明 |
|---|---|
| 内容 | 约 1722 张武器 / 匕首 / 手套贴图 |
| 著作权 | **Valve Corporation** |
| 本仓库处理方式 | **完全排除**，`.gitignore` 已屏蔽 `images/` 与 `cs2-skins/` |
| 使用者须知 | 仅供个人学习研究，**禁止商业用途、禁止再分发** |

程序运行需要这些图片，但**仓库不提供**。获取方式见 README 的「准备图片素材」一节。

> 本项目是**非官方粉丝作品**，与 Valve Corporation 无任何关联，
> 未获得 Valve 的授权、赞助或认可。
> Counter-Strike 是 Valve Corporation 的商标。

---

## 二、BUFF 市场数据（未包含）

| 项目 | 说明 |
|---|---|
| 内容 | `buff_raw.jsonl`、`buff_tx.jsonl`（8MB+）、`prices_template.csv` |
| 来源 | 从 buff.163.com 抓取的挂单价与成交记录 |
| 本仓库处理方式 | **排除**（体积大，且属第三方平台数据） |
| 使用者须知 | 请自行评估目标平台的服务条款与当地法律；**抓取频率务必克制** |

仓库保留了**抓取与加工脚本**（`tools/`），需要数据时自行运行生成。
最终产物 `data/prices.json` 是由上述原始数据聚合而成的价格表，同样不在仓库中。

---

## 三、皮肤元数据（未包含）

| 文件 | 说明 |
|---|---|
| `manifest.json` / `skins.json` / `skins-zh.json` | 约 5MB×2 + 0.9MB，皮肤名称、磨损区间、稀有度等元数据 |

这些是构建 `data/items.json` 的输入。本仓库未包含，请自行准备后运行 `build_items.py`。

---

## 四、原创内容（已包含，受 MIT 许可）

| 项目 | 说明 |
|---|---|
| `src/*.cs` | 全部 C# 源码，无第三方依赖（JSON 解析器也是手写的） |
| `build.ps1` / `build_items.py` | 构建脚本 |
| `tools/*.py` | 数据抓取与加工工具 |
| `data/quiz.txt` | 游戏知识题库（232 道，自行整理编写） |
| `assets/ui/mascot_*.png` | 界面立绘（自制） |
| `assets/ui/bg_blue.png` | 背景图（由立绘合成） |
| `assets/art/001.png`、`002.png` | 立绘原图（自制） |

### 音频

**全部由代码合成波形，不内嵌任何外部音频素材**，
不涉及第三方音频版权。转动音乐、出货音效、特效音均在 `src/Music.cs`、
`src/SoundFx.cs`、`src/DeltaSfx.cs` 中以算法生成。

---

## 五、关于 `data/quiz.txt` 中的题目

题库内容为**游戏剧情与角色的知识问答**，题目与解析均为本项目自行编写整理。
涉及的动画、游戏作品名称与其角色名称，著作权归各自权利人所有，
此处仅用于**非商业性的知识问答**。

若有权利人认为某条内容不妥，欢迎提 Issue，我们会及时删除。

---

## 六、免责声明

本项目为**模拟器**，金币、抽奖、汰换均为**虚拟机制**，
不涉及任何真实货币、真实交易或真实饰品。

项目中的价格数据仅用于模拟演示，**不构成任何投资建议**。
